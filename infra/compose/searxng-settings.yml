# SearXNG settings for the zhiban personal assistant.
# Enables the JSON output format used by SearXNGAdapter.
use_default_settings: true

server:
  secret_key: "zhiban-local-searxng-secret-change-me"
  limiter: false
  image_proxy: false
  port: 8080
  bind_address: "0.0.0.0"

search:
  formats:
    - html
    - json
  safe_search: 0
  autocomplete: ""
  default_lang: "zh-CN"

engines:
  # Keep a small set of reliable, no-API-key engines reachable from CN networks.
  # google/bing are removed: they frequently time out behind CN network egress.
  - name: baidu
    disabled: false
  - name: duckduckgo
    disabled: false
  - name: wikipedia
    disabled: false
