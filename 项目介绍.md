# 知伴（ZhiBan）· 项目介绍

> 一个具备「可控记忆 + 可靠工具调用 + 多轮对话」能力的个人 AI 助理，从 0 到 1 全栈实现。

---

## 一、一句话定位

知伴是一个**能记住你、会调用工具、支持多轮对话**的个人 AI Agent。与普通聊天机器人最大的不同在于：它不只是"回答问题"，而是会**主动理解你的话、把重要信息沉淀成结构化记忆**，并在后续对话中**通过语义检索想起来**，从而把一次次的对话，变成可以延续的理解。

---

## 二、核心能力

| 能力 | 说明 |
|------|------|
| 🧠 **可控记忆** | 主动记忆（你说"记住"）+ 自动记忆（后台从对话中自动提取），按类别展示，用户可编辑/遗忘 |
| 🧬 **记忆自我整合与演化** | 记忆冲突自动保留演化历史（"吃辣"→"不吃辣"），后台定期整理去冗余、消解矛盾，形成凝练画像 |
| 🔍 **可解释的语义检索** | 混合检索（向量 + 中文分词词法）+ 可解释打分，换个问法也能召回，并能说明"为什么想起这条" |
| 🔧 **可靠工具调用** | Function Calling 完整链路：注册 → 路由 → 执行 → 幂等 → 审计 |
| 📋 **待办与提醒** | 单次 + 周期提醒（每天/每周），站内 + 浏览器通知 + 邮件三路触达 |
| 🌐 **联网搜索** | 真实联网搜索，结果去重 + 来源可信度分级，优先权威信息 |
| 💬 **多轮对话** | 流式输出、Markdown 渲染、上下文滚动摘要压缩 |

---

## 三、系统架构

### 3.1 总体拓扑

知伴是一个**模块化单体**，部署上分为 `api` 和 `worker` 两种进程，代码按领域分包。

```
浏览器 (Next.js :3000)
   │  REST / SSE
   ▼
FastAPI (api :8000) ──────────────► PostgreSQL + pgvector（唯一事实源）
   │  ├─ Auth（session + HttpOnly Cookie）
   │  ├─ Conversation（会话/消息）
   │  ├─ Agent Runtime（有界 ReAct + 路由）
   │  ├─ Memory / Todo / Search（领域）
   │  ├─ Tool Registry / Executor
   │  └─ LLM Adapter ──► DeepSeek
   │
   │  写入 jobs / outbox
   ▼
Worker（独立进程，无 HTTP）
   ├─ 记忆抽取 / Embedding
   ├─ 摘要压缩
   ├─ 提醒调度（周期提醒 + 邮件投递）
   └─ jobs/outbox 消费（租约、重试、幂等）

Redis：限流、短租约锁、SSE 事件缓冲、任务通知（可降级到 PG 轮询）
搜索服务：Bocha Search API（可降级 SearXNG / Mock）
```

### 3.2 主 Agent - Subagent 架构（核心设计）

这是项目的架构亮点。主 Agent 只负责**路由决策 + 管理生命周期**，具体事务交给专业 Subagent：

| Subagent | 职责 | 拥有的工具 |
|----------|------|-----------|
| `MemoryAgent` | 记忆召回 + 增删改查 + 整理 | `memory.list/add/update/delete/consolidate` |
| `TaskAgent` | 待办 + 提醒 | `todo.create/complete`、`reminder.create/cancel` |
| `SearchAgent` | 联网检索 | `web_search` |
| 主 Agent 兜底 | 闲聊/解释 + 轻量只读 | `current_time`、`summary` |

**关键设计**：Subagent 返回**结构化摘要**（而非原始对话），其内部推理不会污染主 Agent 上下文，实现了"关注点分离 + 上下文隔离"。

**路由流程**：主 Agent 通过一次轻量 LLM 调用判断用户意图，动态路由到对应 Subagent（而非硬编码 if-else）。

### 3.3 有界 ReAct 循环

主 Agent 的 ReAct 循环是**有界**的，防止失控：

- 最多 4 轮工具调用
- 总超时 60 秒
- 重复工具调用检测
- 空回复兜底

这些约束保证了 Agent 在真实生产环境中**可控、可预测**。

---

## 四、记忆系统（差异化核心）

### 4.1 两种记忆来源

| 类型 | 触发方式 | 处理链路 |
|------|----------|----------|
| **主动记忆** | 用户说"记住…" | 主 Agent 同步写入 |
| **自动记忆** | 自然聊天透露信息 | Worker 后台异步 LLM 提取 |

### 4.2 记忆生命周期

```
用户消息 → 异步记忆抽取（Worker job）
  → LLM 提取候选（严格 JSON）
  → 确定性校验（敏感词/置信度/证据/值格式）
  → 决策（去重/冲突/新增）
  → 持久化（含 Embedding）
```

### 4.3 可解释的混合检索（差异化亮点）

知伴的记忆检索不是简单的关键词匹配，而是一个**可评估、可调优、可解释**的检索系统：

**混合召回 + 六因子打分**：
```
score = 向量相似度 + 词法相关 + 时效衰减 + 重要度 + 置信度 + 类型
```
- **向量召回**：Embedding（bge-m3，1024 维）+ pgvector，解决"换个问法也能想起来"（"喝咖啡偏好" → "美式咖啡不加糖"）
- **词法召回**：jieba 中文分词 + BM25，解决"字面相关"匹配
- **时效衰减**：借鉴遗忘曲线，越久远越不重要的记忆自然沉下去

**数据驱动调优（量化指标）**：
- 建立检索评测集（覆盖语义相关、词法相关、多答案、负例）
- 用 recall@k / nDCG / MRR 量化效果，网格搜索自动调参
- 优化结果：`recall@3` 从 **0.400 提升到 0.900**（+125%），词法降级从 0 到 0.700

**可解释性**：
- 每次召回都输出各因子的得分 breakdown，前端展示"召回了哪些记忆 + 相关度"
- 用户能理解"AI 为什么想起了这条"，而非黑盒

**健壮性**：
- Embedding 服务不可用时自动降级为词法检索
- 修复了向量为 NULL 时检索崩溃的问题

### 4.4 记忆自我整合与演化（差异化亮点）

知伴的记忆不只是"记住"，还会**随用户变化而演化、随积累而自我整理**——这是对照小 Q 的 C2C 关系型记忆取舍后，针对"单用户自我沉淀"场景做的更自由、更有深度的设计：

**① 记忆演化链（事实更新不丢历史）**

当用户改变一个既定偏好（"我原来不吃辣" → "现在能吃一点辣了"），系统**不会覆盖丢失旧值**，而是：

```
旧记忆「不吃辣」──superseded_by──► 新记忆「能吃辣」
        （标记 superseded）              （active）
```

演化历史被完整保留，Agent 能回答"你之前……现在……"的时间追问，而非把用户当成一个静止不变的画像。

**② 记忆整合（自动去冗余 + 消解矛盾）**

当记忆积累到一定量（默认 15 条），后台 Worker 会触发一次**记忆整理**：

```
零散记忆 → LLM 识别「冗余（咖啡 / 喝咖啡）+ 矛盾（住北京 / 住上海）」
         → 输出「保留谁、淘汰谁」的提议
         → 工程层确定性校验后落地（复用 superseded_by 演化链）
```

- **自动触发**：每轮对话记忆抽取后，记忆数超阈值自动整理
- **主动触发**：用户说"整理一下我的记忆"即触发

**核心治理思想（LLM 提议 + 工程裁决）**：

与记忆写入一脉相承——**LLM 只负责"提议"哪些记忆冗余/矛盾，确定性代码负责"裁决"**（校验 id 真实存在、属于该用户、非自指），最后通过演化链落地。这让记忆在"自我进化"的同时，**不会引入 LLM 幻觉**——绝不新造内容、绝不直接改库，宁缺毋滥。

### 4.5 自然语言记忆范式 + 语义去重（三期重构）

二期上线后实测发现「结构化三元组抽取畸形」「主动/自动重复」「正反偏好无法演化」三个更深层问题，遂做了一次**存储范式重构**：

**① 自然语言 fact 范式（根治抽取畸形）**

早期用 `subject/predicate/value` 三元组存储，要求 LLM 做语义解析，极易产出畸形结果：

```
❌ 用户 喜欢吃 用户 不喜欢吃 辣（自我引用）
❌ 用户 喜欢 不吃辣（否定错位）
```

重构后改为**一句自然语言 fact**：

```
✅ 用户不喜欢吃辣
```

LLM 只写一句话，不再拆字段，渲染自然、抽取稳定。

**② 语义去重（主动 + 自动不重复）**

主动记忆（`memory.add`）和自动提取（`flush`）是两条独立路径，之前会各记一条。现在：

- **写入时去重**：自动提取前，与已有记忆做词法相似度比对（含否定极性判断），已记过的不再记
- **定期去重**：后台整合任务增加确定性去重阶段，词法相似度 ≥ 阈值就合并
- **否定极性**：区分「喜欢」vs「不喜欢」——同义去重、反义保留，避免正反偏好被误合并

**③ 主动记忆语义调和（reconcile）**

用户明确说"记住 X"时，把新事实 + 现有记忆喂给模型，由模型判断四选一：

| action | 含义 | 示例 |
|---|---|---|
| `add` | 新增 | 「喜欢香菜」全新信息 |
| `update` | 更新旧值 | 「非常喜欢香菜」补充措辞 |
| `supersede` | 正反取代 | 「不喜欢香菜」反转 |
| `ignore` | 重复忽略 | 「喜欢香菜」已记过 |

模型只"提议"，工程层校验 target 记忆真实存在才执行——既让记忆"智能更新"，又杜绝幻觉误删。

---

## 五、技术栈

| 层 | 技术 |
|----|------|
| 前端 | Next.js 16 · React 19 · TypeScript · Tailwind CSS v4 · shadcn/ui |
| 后端 | Python 3.12 · FastAPI · Pydantic v2 · SQLAlchemy 2 |
| LLM | DeepSeek（`deepseek-v4-flash` / `deepseek-v4-pro`） |
| Embedding | BAAI/bge-m3（1024 维，国内直连） |
| 存储 | PostgreSQL + pgvector |
| 缓存/队列 | Redis |
| 后台任务 | 独立 Worker（jobs/outbox 模式） |
| 联网搜索 | Bocha Search API |
| 测试 | pytest · vitest · Playwright |

---

## 六、工程亮点

1. **Spec 驱动开发**：每个功能先写 Spec（设计文档），再实现、测试、验证，形成完整可追溯的开发链路。

2. **两段式聊天 + SSE 流式**：先 POST 创建消息（幂等），再 GET 拉流式结果，避免断线重连导致消息重复。

3. **可靠性设计**：
   - 后台任务用 PostgreSQL jobs + outbox 模式，租约 + 重试 + 幂等消费
   - 各依赖故障均有降级策略（LLM/Embedding/搜索/Redis 都不可用时仍能优雅降级）

4. **安全设计**：
   - 密码 Argon2id 哈希，session + HttpOnly Cookie
   - 所有数据强制 `user_id` 隔离（跨用户访问返回 404）
   - Prompt Injection 防护、SSRF 规避、日志脱敏

5. **可测试性**：全栈自动化测试覆盖（pytest + vitest + Playwright 端到端）。

6. **数据驱动的检索优化**：为记忆检索建立评测集（recall@k / nDCG / MRR），用网格搜索自动调优打分权重与阈值，替代人工"拍脑袋"，检索效果量化可验证。

7. **搜索质量工程**：联网搜索结果去重（URL 归一化 + 标题相似度）+ 来源可信度分级（权威媒体 > 社区 > 个人博客），让 AI 优先采用权威信息。

8. **记忆自我整合（LLM 提议 + 工程裁决）**：记忆冲突更新保留演化链（`superseded_by_id` 串联历史），后台 `memory.consolidate` 任务自动识别冗余与矛盾、消解后形成凝练画像。全程 LLM 只提议、确定性代码裁决落地，杜绝幻觉，体现"记忆会随用户演化、自我整理"的深度设计。

9. **自然语言记忆范式 + 语义去重**：从易出错的三元组存储重构为自然语言 `fact`，根治抽取畸形；用词法相似度（含否定极性判断）做写入时/定期去重，杜绝主动+自动提取重复；主动记忆走 `reconcile` 语义调和（模型判断 add/update/supersede/ignore + 工程裁决），实现正反偏好的智能演化。

---

## 七、项目结构

```
apps/
  api/          # 后端（FastAPI，模块化单体）
  web/          # 前端（Next.js）
packages/
  contracts/    # OpenAPI + TypeScript 契约
infra/          # Docker Compose、Dockerfile
scripts/        # 开发/检查/seed 脚本
specs/          # Spec 驱动实施文档
docs/           # 设计文档 + 现状文档
```

---

## 八、总结

知伴完整实现了一个生产级的个人 AI Agent，从**前端交互、后端架构、Agent 运行时、记忆系统、工具调用、后台任务**到**安全与可靠性**，形成了闭环。其中：

- **记忆系统**是差异化亮点 —— 主动 + 自动双层记忆，加上可解释的混合检索，让 AI 真正"记住"用户、且能说明"为什么想起"；
- **记忆的自我整合与演化**体现了深度 —— 记忆不只是静态快照，而是会随用户变化保留演化历史、随积累自我整理去冗余消矛盾，形成凝练画像；
- **检索优化**体现了深度 —— 从"拍脑袋的固定权重"升级为"可评估、可调优的检索系统"，recall@3 提升 125%；
- **主 Agent - Subagent 架构**体现了对 Agent 设计的深入思考 —— 路由、上下文隔离、有界循环；
- **工程完整性** —— 从 Spec 驱动、自动化测试（150+ 用例）到降级兜底，展示了扎实的工程能力。

---


